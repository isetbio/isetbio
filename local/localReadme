This directory is used as scratch space for downloads, and it is also a good place to keep various personalizations, such as your validation preference file.  

Only this file and the directory are kept in the repository.  Other files are ignored.

