function [list] = RemoveListDups(list,ndim)% [list] = RemoveListDups(list,ndim)%% Remove any duplicates in the receptor list by bumping% by one pixel.  Loops until there are no duplicates.  On% a sufficiently dense grid this could hang, but it seems% unlikely in practice.%% 12/26/04	dhb		Removed old style call to rand('uniform');done = 0;while (done == 0)  done = 1;  list = SortList(list);  for i=1:length(list)-1    j = i+1;    if (list(i,:) == list(j,:))      list(j,:) = list(j,:) + sign( rand(1,2) - 0.5 );      done = 0;    end  endend