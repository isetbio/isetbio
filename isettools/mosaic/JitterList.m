function [list] = JitterList(list,sd,ndim)% [list] = JitterList(list,sd,ndim)%% Jitter the positions of the elements of list% by adding Gaussian noise.  Jitter with wrap-around% so that the members of the list stay in the same% area.%% 11/23/93  dhb  Upgraded random generation for 4.1 style.%                Not backwards compatible.list = list + sd*randn(size(list));list = IntegerList(list);list = TruncateList(list,1,ndim);