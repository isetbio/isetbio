function [R] = ListToRMat(list,ndim)% [R] = ListToRMat(list,ndim)%% Creates a two-dimensional receptor matrix% based on the passed of (y,x) receptor positions.%% 7/10/94		dhb		Changed name from MakeReceptorMat.% Create space for R matrixnrec = length(list);R = zeros(nrec,ndim*ndim);				% Go through list and put in a 1 at% all appropriate locations for i = 1:nrec	% Allocate space and set crit value to 1	% Could convolve in here if desired  rfunc = zeros(ndim,ndim);	rfunc(list(i,1),list(i,2)) = 1.0;	% Stuff image into a single row	R(i,:) = ImageToVec(rfunc)';end