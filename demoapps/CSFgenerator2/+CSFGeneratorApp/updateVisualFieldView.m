function updateVisualFieldView(app, mode)

    switch (mode)
        case 'initialize'
            initializeVisualFieldView(app);
        case 'update'
            updateVisualFieldViewWithNewData(app);
    end
end


function initializeVisualFieldView(app)

end

function updateVisualFieldViewWithNewData(app)

end
